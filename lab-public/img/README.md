# Lab 图片

这里放 Lab 入口页用到的封面图。当前文件是从本地 preview 截取的 16:9 游戏截图，可直接替换为后续正式封面。

入口页引用：

- `/img/race-cover.png`
- `/img/throw-battle-cover.png`
- `/img/little-travel-cat-cover.png`

如果替换为 `jpg`、`png` 或 `webp`，请同步修改 `lab-src/pages/index.astro` 中 `projects` 数组里的 `image` 路径。
